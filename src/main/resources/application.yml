
server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/jwt
    username: root
    password: admin
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update


javatar:
  app:
    jwtSecret: ======================Javatar=Spring===========================
    jwtExpirationMs: 86400000
